/**
 * URLs das redes sociais da G2 Tech Hub.
 *
 * IMPORTANTE: os valores abaixo estão vazios propositalmente — nenhuma URL
 * foi fornecida na especificação do projeto. Preencha com os perfis reais
 * antes de publicar em produção. Enquanto vazios, os respectivos ícones no
 * rodapé não são renderizados.
 */

type SocialConfig = {
  instagram: string
  linkedin: string
}

export const socialConfig: SocialConfig = {
  instagram: '',
  linkedin: '',
}

export type { SocialConfig }
